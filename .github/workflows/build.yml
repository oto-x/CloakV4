name: Build CloakV4 Android APK

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Repository with Submodules
      uses: actions/checkout@v4
      with:
        submodules: recursive
        fetch-depth: 0

    - name: Install System Build Dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y gettext cmake ninja-build build-essential curl unzip

    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'

    - name: Setup Android SDK
      uses: android-actions/setup-android@v3

    - name: Setup Android NDK
      uses: nttld/setup-ndk@v1
      id: setup-ndk
      with:
        ndk-version: r25c
        link-to-sdk: true

    - name: Download Native C++ Dependencies
      run: |
        mkdir -p android/deps
        curl -L -o deps.zip https://github.com/luanti-org/luanti_android_deps/releases/download/latest/deps-lite.zip
        unzip -q deps.zip -d android/deps
        rm deps.zip

    - name: Build C++ Native Binaries & APK
      env:
        ANDROID_NDK_HOME: ${{ steps.setup-ndk.outputs.ndk-path }}
      run: |
        cd android
        chmod +x gradlew
        ./gradlew assembleDebug -PabiFilters=arm64-v8a -PBUILD_Luanti=TRUE --stacktrace

    - name: Upload ARM64 APK Artifact
      uses: actions/upload-artifact@v4
      with:
        name: CloakV4-ARM64-Debug
        path: android/app/build/outputs/apk/debug/app-arm64-v8a-debug.apk
